import 'package:flutter/material.dart';

class ButtonConstants {
  static const double defaultPadding = 22.0;
  static const Color defaultTextColor = Colors.white;

  static Color? defaultButtonColor = Colors.grey[850];
}
